// const router = require('express').Router();
// const deliveryController = require('../controllers/delivery');
// const onlyLoggedIn = require('../lib/only-logged-in');

// module.exports = () => {
//   router.post('/route', onlyLoggedIn, deliveryController.createRoute);
//   router.get('/routes', onlyLoggedIn, deliveryController.getRoutes);
//   router.delete('/route', onlyLoggedIn, deliveryController.deleteRoute);
//   return router;
// };
